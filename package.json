{
  "name": "todo-react-router",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "json-server": "^0.9.4",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "server": "json-server -p 8080 --watch db.json",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
